const express=require('express');
const path=require('path');const fs=require('fs');const bcrypt=require('bcryptjs');const jwt=require('jsonwebtoken');const cookieParser=require('cookie-parser');const multer=require('multer');const Database=require('better-sqlite3');
const app=express();
app.set('trust proxy',1);
app.disable('x-powered-by');
const PORT=process.env.PORT||3000;
const NODE_ENV=process.env.NODE_ENV||'development';
const JWT_SECRET=process.env.JWT_SECRET;
if(NODE_ENV==='production' && (!JWT_SECRET || JWT_SECRET.length<32)){throw new Error('Production requires a JWT_SECRET of at least 32 characters.');}
const EFFECTIVE_JWT_SECRET=JWT_SECRET||'development-only-change-me-before-production-123456789';
const ROOT=__dirname;const DB_PATH=process.env.DATABASE_PATH||path.join(ROOT,'data/thebg.sqlite');fs.mkdirSync(path.dirname(DB_PATH),{recursive:true});fs.mkdirSync(path.join(ROOT,'public/uploads'),{recursive:true});
const db=new Database(DB_PATH);db.pragma('foreign_keys=ON');
db.exec(`CREATE TABLE IF NOT EXISTS departments(id TEXT PRIMARY KEY,position TEXT NOT NULL,person TEXT NOT NULL,responsibility TEXT NOT NULL);CREATE TABLE IF NOT EXISTS users(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,username TEXT UNIQUE NOT NULL,password_hash TEXT NOT NULL,department_id TEXT NOT NULL,active INTEGER DEFAULT 1,created_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(department_id) REFERENCES departments(id));CREATE TABLE IF NOT EXISTS motorcycles(id INTEGER PRIMARY KEY AUTOINCREMENT,code TEXT UNIQUE NOT NULL,plate TEXT,model TEXT,purchase_date TEXT,purchase_price REAL DEFAULT 0,status TEXT DEFAULT 'Active',created_at TEXT DEFAULT CURRENT_TIMESTAMP);CREATE TABLE IF NOT EXISTS assignments(id INTEGER PRIMARY KEY AUTOINCREMENT,motorcycle_id INTEGER NOT NULL,rider_name TEXT NOT NULL,start_date TEXT NOT NULL,end_date TEXT,notes TEXT,FOREIGN KEY(motorcycle_id) REFERENCES motorcycles(id));CREATE TABLE IF NOT EXISTS income(id INTEGER PRIMARY KEY AUTOINCREMENT,date TEXT NOT NULL,motorcycle_id INTEGER NOT NULL,amount REAL NOT NULL,collection_note TEXT,entered_by INTEGER NOT NULL,verified INTEGER DEFAULT 0,created_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(motorcycle_id) REFERENCES motorcycles(id),FOREIGN KEY(entered_by) REFERENCES users(id));CREATE TABLE IF NOT EXISTS expenses(id INTEGER PRIMARY KEY AUTOINCREMENT,date TEXT NOT NULL,motorcycle_id INTEGER,expense_type TEXT NOT NULL,amount REAL NOT NULL,description TEXT,entered_by INTEGER NOT NULL,created_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(motorcycle_id) REFERENCES motorcycles(id),FOREIGN KEY(entered_by) REFERENCES users(id));CREATE TABLE IF NOT EXISTS tasks(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,responsible_user INTEGER NOT NULL,start_date TEXT,deadline TEXT,priority TEXT DEFAULT 'Normal',description TEXT,status TEXT DEFAULT 'Not Started',created_by INTEGER NOT NULL,created_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(responsible_user) REFERENCES users(id),FOREIGN KEY(created_by) REFERENCES users(id));CREATE TABLE IF NOT EXISTS activities(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL,date TEXT NOT NULL,done TEXT,unfinished TEXT,reason TEXT,time_spent REAL,evidence_id INTEGER,created_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(user_id) REFERENCES users(id));CREATE TABLE IF NOT EXISTS reports(id INTEGER PRIMARY KEY AUTOINCREMENT,type TEXT NOT NULL,body TEXT NOT NULL,user_id INTEGER NOT NULL,date TEXT NOT NULL,status TEXT DEFAULT 'Submitted',created_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(user_id) REFERENCES users(id));CREATE TABLE IF NOT EXISTS goals(id INTEGER PRIMARY KEY AUTOINCREMENT,title TEXT NOT NULL,scope TEXT NOT NULL,department_id TEXT,target REAL DEFAULT 100,achieved REAL DEFAULT 0,period TEXT,created_by INTEGER NOT NULL,FOREIGN KEY(department_id) REFERENCES departments(id),FOREIGN KEY(created_by) REFERENCES users(id));CREATE TABLE IF NOT EXISTS maintenance(id INTEGER PRIMARY KEY AUTOINCREMENT,motorcycle_id INTEGER NOT NULL,issue TEXT NOT NULL,date TEXT NOT NULL,mileage REAL,parts TEXT,cost REAL DEFAULT 0,garage TEXT,next_service TEXT,downtime REAL DEFAULT 0,status TEXT DEFAULT 'Completed',FOREIGN KEY(motorcycle_id) REFERENCES motorcycles(id));CREATE TABLE IF NOT EXISTS odometer(id INTEGER PRIMARY KEY AUTOINCREMENT,motorcycle_id INTEGER NOT NULL,date TEXT NOT NULL,mileage REAL NOT NULL,entered_by INTEGER NOT NULL,FOREIGN KEY(motorcycle_id) REFERENCES motorcycles(id),FOREIGN KEY(entered_by) REFERENCES users(id));CREATE TABLE IF NOT EXISTS evidence(id INTEGER PRIMARY KEY AUTOINCREMENT,filename TEXT NOT NULL,original_name TEXT NOT NULL,mime TEXT,uploaded_by INTEGER NOT NULL,uploaded_at TEXT DEFAULT CURRENT_TIMESTAMP,task_id INTEGER,report_id INTEGER,FOREIGN KEY(uploaded_by) REFERENCES users(id));CREATE TABLE IF NOT EXISTS finance_changes(id INTEGER PRIMARY KEY AUTOINCREMENT,record_type TEXT NOT NULL,record_id INTEGER NOT NULL,original_json TEXT NOT NULL,proposed_json TEXT NOT NULL,reason TEXT NOT NULL,status TEXT DEFAULT 'Pending Approval',requested_by INTEGER NOT NULL,decided_by INTEGER,decision_note TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP,decided_at TEXT,FOREIGN KEY(requested_by) REFERENCES users(id),FOREIGN KEY(decided_by) REFERENCES users(id));CREATE TABLE IF NOT EXISTS audit(id INTEGER PRIMARY KEY AUTOINCREMENT,action TEXT NOT NULL,record_type TEXT NOT NULL,record_id INTEGER,original_json TEXT,changed_json TEXT,reason TEXT,who_user INTEGER,when_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(who_user) REFERENCES users(id));`);
const departments=[['D1','Chairman & CEO','MANISHIMWE FARADJI','Leadership, vision, major decisions & unity'],['D2','Vice Chairman & Deputy CEO','AHMED FAZZIR','Strategy, market research, opportunities & implementation'],['D3','Finance & Assets Officer (CFO)','NIYITANGA OSAMA','Finance, assets, taxes, financial records, financial verification & financial reconciliation'],['D4','Operations Officer (COO)','KIREZI NASSIB','Daily operations, workers, suppliers, quality, efficiency, motorcycle operations & motorcycle workers / riders'],['D5','Legal & Documentation Officer (CLO)','IMANANIYOGISUBIZO YUSSUF','Legal protection, contracts & documentation']];
const upDept=db.prepare('INSERT OR IGNORE INTO departments(id,position,person,responsibility) VALUES(?,?,?,?)');departments.forEach(d=>upDept.run(...d));
const userCount=oneCount();
function oneCount(){return db.prepare('SELECT COUNT(*) n FROM users').get().n}
const initialPassword=process.env.INITIAL_ADMIN_PASSWORD;
if(userCount===0){
  if(NODE_ENV==='production' && (!initialPassword || initialPassword.length<10)) throw new Error('First production startup requires INITIAL_ADMIN_PASSWORD of at least 10 characters.');
  const seedPassword=initialPassword||'1234';
  const upUser=db.prepare('INSERT INTO users(name,username,password_hash,department_id) VALUES(?,?,?,?)');
  departments.forEach(d=>{const username=d[0].toLowerCase();upUser.run(d[2],username,bcrypt.hashSync(seedPassword,12),d[0]);});
  if(NODE_ENV==='development') console.warn('Development demo accounts created with password 1234. Never use this in production.');
}
app.use(express.json({limit:'2mb'}));
app.use(express.urlencoded({extended:true, limit:'2mb'}));
app.use(cookieParser());
// Security headers (no external dependency required)
app.use((req,res,next)=>{res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('X-Frame-Options','SAMEORIGIN');res.setHeader('Referrer-Policy','strict-origin-when-cross-origin');res.setHeader('Permissions-Policy','camera=(), microphone=(), geolocation=()');if(NODE_ENV==='production')res.setHeader('Strict-Transport-Security','max-age=31536000; includeSubDomains');next()});
// Small in-memory login rate limiter; reset on process restart.
const loginAttempts=new Map();
function loginGuard(req,res,next){const key=String(req.ip||'unknown');const now=Date.now();let x=loginAttempts.get(key)||{n:0,t:now};if(now-x.t>15*60*1000)x={n:0,t:now};if(x.n>=10)return res.status(429).json({error:'Too many login attempts. Try again later.'});req._loginKey=key;req._loginState=x;next()}

const allowedMime=new Set(['image/jpeg','image/png','image/webp','application/pdf','text/plain','application/msword','application/vnd.openxmlformats-officedocument.wordprocessingml.document','application/vnd.ms-excel','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet']);
const upload=multer({fileFilter:(req,file,cb)=>cb(null,allowedMime.has(file.mimetype)),storage:multer.diskStorage({destination:path.join(ROOT,'public/uploads'),filename:(req,file,cb)=>cb(null,Date.now()+'-'+Math.random().toString(36).slice(2)+path.extname(file.originalname))}),limits:{fileSize:10*1024*1024}});
function auth(req,res,next){try{const token=req.cookies.bg_token;if(!token)throw 0;req.user=jwt.verify(token,EFFECTIVE_JWT_SECRET);next();}catch(e){res.status(401).json({error:'Not authenticated'});}}
function d1(req){return req.user.department_id==='D1'}function log(req,action,type,id,original=null,changed=null,reason=''){db.prepare('INSERT INTO audit(action,record_type,record_id,original_json,changed_json,reason,who_user) VALUES(?,?,?,?,?,?,?)').run(action,type,id,original?JSON.stringify(original):null,changed?JSON.stringify(changed):null,reason,req.user.id)}
function rows(sql,...p){return db.prepare(sql).all(...p)}function one(sql,...p){return db.prepare(sql).get(...p)}
app.post('/api/login',loginGuard,(req,res)=>{const username=String(req.body.username||'').trim().toLowerCase();const password=String(req.body.password||'');const u=one('SELECT * FROM users WHERE username=? AND active=1',username);if(!u||!bcrypt.compareSync(password,u.password_hash)){req._loginState.n++;loginAttempts.set(req._loginKey,req._loginState);return res.status(401).json({error:'Invalid login'});}loginAttempts.delete(req._loginKey);const token=jwt.sign({id:u.id,name:u.name,username:u.username,department_id:u.department_id},EFFECTIVE_JWT_SECRET,{expiresIn:'7d'});res.cookie('bg_token',token,{httpOnly:true,sameSite:'lax',secure:NODE_ENV==='production',path:'/',maxAge:7*24*3600*1000});res.json({user:{id:u.id,name:u.name,username:u.username,department_id:u.department_id}})});

  const password=String(req.body.password||'');
  const u=one('SELECT * FROM users WHERE username=? AND active=1',username);
  if(!u||!bcrypt.compareSync(password,u.password_hash)){
    req._loginState.n++;
    loginAttempts.set(req._loginKey,req._loginState);
    return res.status(401).json({error:'Invalid login'});
  }
  loginAttempts.delete(req._loginKey);
  const token=jwt.sign({id:u.id,name:u.name,username:u.username,department_id:u.department_id},EFFECTIVE_JWT_SECRET,{expiresIn:'7d'});
  res.cookie('bg_token',token,{httpOnly:true,sameSite:'lax',secure:NODE_ENV==='production',path:'/',maxAge:7*24*3600*1000});
  res.json({user:{id:u.id,name:u.name,username:u.username,department_id:u.department_id}});
});
app.post('/api/logout',(req,res)=>{res.clearCookie('bg_token',{httpOnly:true,sameSite:'lax',secure:NODE_ENV==='production'});res.json({ok:true})});
app.get('/api/health',(req,res)=>res.json({ok:true,service:'THE BG WEB',environment:NODE_ENV}));
app.get('/api/me',auth,(req,res)=>res.json({user:req.user,department:one('SELECT * FROM departments WHERE id=?',req.user.department_id)}));
app.get('/api/bootstrap',auth,(req,res)=>{const users=rows('SELECT id,name,username,department_id,active FROM users');res.json({departments:rows('SELECT * FROM departments'),users,motorcycles:rows('SELECT * FROM motorcycles ORDER BY id DESC'),tasks:rows('SELECT t.*,u.name responsible_name FROM tasks t JOIN users u ON u.id=t.responsible_user ORDER BY t.id DESC'),reports:rows('SELECT r.*,u.name user_name FROM reports r JOIN users u ON u.id=r.user_id ORDER BY r.id DESC'),income:rows('SELECT i.*,m.code motorcycle_code,u.name entered_name FROM income i JOIN motorcycles m ON m.id=i.motorcycle_id JOIN users u ON u.id=i.entered_by ORDER BY i.date DESC,i.id DESC'),expenses:rows('SELECT e.*,m.code motorcycle_code,u.name entered_name FROM expenses e LEFT JOIN motorcycles m ON m.id=e.motorcycle_id JOIN users u ON u.id=e.entered_by ORDER BY e.date DESC,e.id DESC'),maintenance:rows('SELECT m.*,x.code motorcycle_code FROM maintenance m JOIN motorcycles x ON x.id=m.motorcycle_id ORDER BY m.date DESC'),audit:rows('SELECT a.*,u.name user_name FROM audit a LEFT JOIN users u ON u.id=a.who_user ORDER BY a.id DESC LIMIT 1000'),changes:rows('SELECT f.*,u.name requested_name FROM finance_changes f JOIN users u ON u.id=f.requested_by WHERE f.status="Pending Approval" ORDER BY f.id DESC')})});
function sameDept(req,userId){const u=one('SELECT department_id FROM users WHERE id=?',userId);return d1(req)||u?.department_id===req.user.department_id}
app.post('/api/motorcycles',auth,(req,res)=>{if(!['D1','D4'].includes(req.user.department_id))return res.status(403).json({error:'D4 Operations or D1 only'});const {code,plate,model,purchase_date,purchase_price,status='Active'}=req.body;if(!code)return res.status(400).json({error:'Motorcycle code required'});try{const r=db.prepare('INSERT INTO motorcycles(code,plate,model,purchase_date,purchase_price,status) VALUES(?,?,?,?,?,?)').run(code,plate||'',model||'',purchase_date||null,Number(purchase_price||0),status);log(req,'CREATE','Motorcycle',r.lastInsertRowid,null,req.body);res.json({ok:true,id:r.lastInsertRowid})}catch(e){res.status(400).json({error:e.message})}});
app.post('/api/income',auth,(req,res)=>{if(!['D1','D4'].includes(req.user.department_id))return res.status(403).json({error:'Daily motorcycle income is entered by D4 Operations.'});const {date,motorcycle_id,amount,collection_note=''}=req.body;if(!date||!motorcycle_id||Number(amount)<0)return res.status(400).json({error:'Date, motorcycle and valid amount required'});const r=db.prepare('INSERT INTO income(date,motorcycle_id,amount,collection_note,entered_by) VALUES(?,?,?,?,?)').run(date,motorcycle_id,Number(amount),collection_note,req.user.id);log(req,'CREATE','Fleet Income',r.lastInsertRowid,null,req.body);res.json({ok:true,id:r.lastInsertRowid})});
app.post('/api/expenses',auth,(req,res)=>{const {date,motorcycle_id,expense_type,amount,description=''}=req.body;if(!date||!expense_type||Number(amount)<=0)return res.status(400).json({error:'Valid expense required'});const original={date,motorcycle_id:motorcycle_id||null,expense_type,amount:Number(amount),description};const r=db.prepare('INSERT INTO expenses(date,motorcycle_id,expense_type,amount,description,entered_by) VALUES(?,?,?,?,?,?)').run(date,motorcycle_id||null,expense_type,Number(amount),description,req.user.id);log(req,'CREATE','Expense',r.lastInsertRowid,null,original);res.json({ok:true,id:r.lastInsertRowid})});
app.post('/api/tasks',auth,(req,res)=>{const {name,responsible_user,start_date,deadline,priority='Normal',description=''}=req.body;if(!name||!responsible_user)return res.status(400).json({error:'Task and responsible person required'});if(!sameDept(req,responsible_user)&&!d1(req))return res.status(403).json({error:'You can assign tasks only within your department unless you are D1'});const r=db.prepare('INSERT INTO tasks(name,responsible_user,start_date,deadline,priority,description,created_by) VALUES(?,?,?,?,?,?,?)').run(name,responsible_user,start_date||null,deadline||null,priority,description,req.user.id);log(req,'CREATE','Task',r.lastInsertRowid,null,req.body);res.json({ok:true,id:r.lastInsertRowid})});
app.patch('/api/tasks/:id',auth,(req,res)=>{const t=one('SELECT * FROM tasks WHERE id=?',req.params.id);if(!t)return res.status(404).json({error:'Task not found'});if(!d1(req)&&t.responsible_user!==req.user.id)return res.status(403).json({error:'Not permitted'});const status=req.body.status||t.status;db.prepare('UPDATE tasks SET status=? WHERE id=?').run(status,t.id);log(req,'CHANGE','Task',t.id,t,{...t,status});res.json({ok:true})});
app.post('/api/reports',auth,(req,res)=>{const {type,body,date}=req.body;if(!type||!body)return res.status(400).json({error:'Report type and body required'});const r=db.prepare('INSERT INTO reports(type,body,user_id,date) VALUES(?,?,?,?)').run(type,body,req.user.id,date||new Date().toISOString().slice(0,10));log(req,'CREATE','Report',r.lastInsertRowid,null,req.body);res.json({ok:true,id:r.lastInsertRowid})});
app.post('/api/maintenance',auth,(req,res)=>{const {motorcycle_id,issue,date,mileage,parts,cost,garage,next_service,downtime,status='Completed'}=req.body;if(!motorcycle_id||!issue||!date)return res.status(400).json({error:'Motorcycle, issue and date required'});const r=db.prepare('INSERT INTO maintenance(motorcycle_id,issue,date,mileage,parts,cost,garage,next_service,downtime,status) VALUES(?,?,?,?,?,?,?,?,?,?)').run(motorcycle_id,issue,date,mileage||null,parts||'',Number(cost||0),garage||'',next_service||null,Number(downtime||0),status);if(status==='In Progress')db.prepare('UPDATE motorcycles SET status="Under Maintenance" WHERE id=?').run(motorcycle_id);log(req,'CREATE','Maintenance',r.lastInsertRowid,null,req.body);res.json({ok:true,id:r.lastInsertRowid})});
app.post('/api/odometer',auth,(req,res)=>{const {motorcycle_id,date,mileage}=req.body;if(!motorcycle_id||!date||Number(mileage)<0)return res.status(400).json({error:'Invalid odometer'});const prev=one('SELECT mileage FROM odometer WHERE motorcycle_id=? ORDER BY mileage DESC LIMIT 1',motorcycle_id);if(prev&&Number(mileage)<Number(prev.mileage))return res.status(400).json({error:'Mileage cannot go backwards'});const r=db.prepare('INSERT INTO odometer(motorcycle_id,date,mileage,entered_by) VALUES(?,?,?,?)').run(motorcycle_id,date,Number(mileage),req.user.id);log(req,'CREATE','Odometer',r.lastInsertRowid,null,req.body);res.json({ok:true,id:r.lastInsertRowid})});
app.post('/api/activities',auth,(req,res)=>{const {date,done,unfinished,reason,time_spent}=req.body;const r=db.prepare('INSERT INTO activities(user_id,date,done,unfinished,reason,time_spent) VALUES(?,?,?,?,?,?)').run(req.user.id,date||new Date().toISOString().slice(0,10),done||'',unfinished||'',reason||'',Number(time_spent||0));log(req,'CREATE','Daily Activity',r.lastInsertRowid,null,req.body);res.json({ok:true,id:r.lastInsertRowid})});
app.get('/api/activities',auth,(req,res)=>res.json(rows(d1(req)?'SELECT a.*,u.name user_name,u.department_id FROM activities a JOIN users u ON u.id=a.user_id ORDER BY a.id DESC':'SELECT a.*,u.name user_name,u.department_id FROM activities a JOIN users u ON u.id=a.user_id WHERE u.department_id=? ORDER BY a.id DESC',...(d1(req)?[]:[req.user.department_id]))));
app.post('/api/evidence',auth,upload.single('file'),(req,res)=>{if(!req.file)return res.status(400).json({error:'File required'});const r=db.prepare('INSERT INTO evidence(filename,original_name,mime,uploaded_by,task_id,report_id) VALUES(?,?,?,?,?,?)').run(req.file.filename,req.file.originalname,req.file.mimetype,req.user.id,req.body.task_id||null,req.body.report_id||null);log(req,'CREATE','Evidence',r.lastInsertRowid,null,{original_name:req.file.originalname,task_id:req.body.task_id,report_id:req.body.report_id});res.json({ok:true,id:r.lastInsertRowid,url:'/uploads/'+req.file.filename})});
app.get('/api/audit',auth,(req,res)=>{const q=(req.query.q||'').toLowerCase();const data=rows(d1(req)?'SELECT a.*,u.name user_name FROM audit a LEFT JOIN users u ON u.id=a.who_user ORDER BY a.id DESC':'SELECT a.*,u.name user_name FROM audit a LEFT JOIN users u ON u.id=a.who_user WHERE u.department_id=? ORDER BY a.id DESC',...(d1(req)?[]:[req.user.department_id]));res.json(data.filter(x=>JSON.stringify(x).toLowerCase().includes(q)))});
app.post('/api/finance-changes',auth,(req,res)=>{if(!['D3','D4'].includes(req.user.department_id)&&!d1(req))return res.status(403).json({error:'Finance changes are restricted'});const {record_type,record_id,original,proposed,reason}=req.body;if(!reason)return res.status(400).json({error:'Reason is mandatory for critical finance changes'});const r=db.prepare('INSERT INTO finance_changes(record_type,record_id,original_json,proposed_json,reason,requested_by) VALUES(?,?,?,?,?,?)').run(record_type,record_id,JSON.stringify(original),JSON.stringify(proposed),reason,req.user.id);log(req,'PROPOSE_CHANGE',record_type,record_id,original,proposed,reason);res.json({ok:true,id:r.lastInsertRowid})});
app.post('/api/finance-changes/:id/decision',auth,(req,res)=>{if(!d1(req))return res.status(403).json({error:'Only D1 can approve or reject'});const c=one('SELECT * FROM finance_changes WHERE id=?',req.params.id);if(!c||c.status!=='Pending Approval')return res.status(404).json({error:'Change not pending'});const decision=req.body.decision;if(!['Approved','Rejected'].includes(decision))return res.status(400).json({error:'Decision must be Approved or Rejected'});db.prepare('UPDATE finance_changes SET status=?,decided_by=?,decision_note=?,decided_at=CURRENT_TIMESTAMP WHERE id=?').run(decision,req.user.id,req.body.note||'',c.id);log(req,'FINANCE_DECISION',c.record_type,c.record_id,JSON.parse(c.original_json),JSON.parse(c.proposed_json),`${decision}: ${req.body.note||''}`);res.json({ok:true})});
app.get('/api/fleet-summary',auth,(req,res)=>{const income=one('SELECT COALESCE(SUM(amount),0) total FROM income').total;const expense=one('SELECT COALESCE(SUM(amount),0) total FROM expenses WHERE motorcycle_id IS NOT NULL').total;const today=new Date().toISOString().slice(0,10);const ti=one('SELECT COALESCE(SUM(amount),0) total FROM income WHERE date=?',today).total;const te=one('SELECT COALESCE(SUM(amount),0) total FROM expenses WHERE date=? AND motorcycle_id IS NOT NULL',today).total;res.json({totalMotorcycles:one('SELECT COUNT(*) n FROM motorcycles').n,active:one('SELECT COUNT(*) n FROM motorcycles WHERE status="Active"').n,inactive:one('SELECT COUNT(*) n FROM motorcycles WHERE status="Inactive"').n,maintenance:one('SELECT COUNT(*) n FROM motorcycles WHERE status="Under Maintenance"').n,sold:one('SELECT COUNT(*) n FROM motorcycles WHERE status="Sold / Retired"').n,todayIncome:ti,todayExpenses:te,todayNet:ti-te,totalIncome:income,totalExpenses:expense,net:income-expense})});

// --- THE BG WEB v1.0 complete module endpoints ---
db.exec(`CREATE TABLE IF NOT EXISTS daily_closings(id INTEGER PRIMARY KEY AUTOINCREMENT,date TEXT NOT NULL UNIQUE,income REAL DEFAULT 0,expenses REAL DEFAULT 0,net REAL DEFAULT 0,closed_by INTEGER NOT NULL,notes TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(closed_by) REFERENCES users(id));`);
function deptOnly(req, ids){ return d1(req) || ids.includes(req.user.department_id); }
app.get('/api/activities',auth,(req,res)=>{const data=rows(d1(req)?'SELECT a.*,u.name user_name,u.department_id FROM activities a JOIN users u ON u.id=a.user_id ORDER BY a.id DESC':'SELECT a.*,u.name user_name,u.department_id FROM activities a JOIN users u ON u.id=a.user_id WHERE u.department_id=? ORDER BY a.id DESC',...(d1(req)?[]:[req.user.department_id]));res.json(data)});
app.get('/api/goals',auth,(req,res)=>res.json(rows(d1(req)?'SELECT g.*,u.name creator_name FROM goals g JOIN users u ON u.id=g.created_by ORDER BY g.id DESC':'SELECT g.*,u.name creator_name FROM goals g JOIN users u ON u.id=g.created_by WHERE g.department_id=? OR g.department_id IS NULL ORDER BY g.id DESC',...(d1(req)?[]:[req.user.department_id]))));
app.post('/api/goals',auth,(req,res)=>{if(!d1(req)&&!['D2','D3','D4','D5'].includes(req.user.department_id))return res.status(403).json({error:'Not permitted'});const {title,scope='Department',department_id,target=100,achieved=0,period=''}=req.body;if(!title)return res.status(400).json({error:'Title required'});const dept=department_id||req.user.department_id;if(!d1(req)&&dept!==req.user.department_id)return res.status(403).json({error:'Only your department'});const r=db.prepare('INSERT INTO goals(title,scope,department_id,target,achieved,period,created_by) VALUES(?,?,?,?,?,?,?)').run(title,scope,dept,Number(target),Number(achieved),period,req.user.id);log(req,'CREATE','Goal',r.lastInsertRowid,null,req.body);res.json({ok:true,id:r.lastInsertRowid})});
app.patch('/api/goals/:id',auth,(req,res)=>{const g=one('SELECT * FROM goals WHERE id=?',req.params.id);if(!g)return res.status(404).json({error:'Goal not found'});if(!d1(req)&&g.department_id!==req.user.department_id)return res.status(403).json({error:'Not permitted'});const n={...g,achieved:req.body.achieved===undefined?g.achieved:Number(req.body.achieved),title:req.body.title??g.title,target:req.body.target===undefined?g.target:Number(req.body.target)};db.prepare('UPDATE goals SET title=?,target=?,achieved=? WHERE id=?').run(n.title,n.target,n.achieved,g.id);log(req,'CHANGE','Goal',g.id,g,n);res.json({ok:true})});
app.get('/api/assignments',auth,(req,res)=>res.json(rows(d1(req)?'SELECT a.*,m.code motorcycle_code FROM assignments a JOIN motorcycles m ON m.id=a.motorcycle_id ORDER BY a.id DESC':'SELECT a.*,m.code motorcycle_code FROM assignments a JOIN motorcycles m ON m.id=a.motorcycle_id WHERE m.id IN (SELECT motorcycle_id FROM assignments WHERE id=a.id) ORDER BY a.id DESC')));
app.post('/api/assignments',auth,(req,res)=>{if(!deptOnly(req,['D4']))return res.status(403).json({error:'D4 Operations or D1 only'});const {motorcycle_id,rider_name,start_date,end_date,notes=''}=req.body;if(!motorcycle_id||!rider_name||!start_date)return res.status(400).json({error:'Motorcycle, rider and start date required'});db.prepare('UPDATE assignments SET end_date=? WHERE motorcycle_id=? AND end_date IS NULL').run(start_date,motorcycle_id);const r=db.prepare('INSERT INTO assignments(motorcycle_id,rider_name,start_date,end_date,notes) VALUES(?,?,?,?,?)').run(motorcycle_id,rider_name,start_date,end_date||null,notes);log(req,'CREATE','Rider Assignment',r.lastInsertRowid,null,req.body);res.json({ok:true,id:r.lastInsertRowid})});
app.get('/api/odometer',auth,(req,res)=>res.json(rows('SELECT o.*,m.code motorcycle_code,u.name entered_name FROM odometer o JOIN motorcycles m ON m.id=o.motorcycle_id JOIN users u ON u.id=o.entered_by ORDER BY o.date DESC,o.id DESC')));
app.get('/api/maintenance',auth,(req,res)=>res.json(rows('SELECT m.*,x.code motorcycle_code FROM maintenance m JOIN motorcycles x ON x.id=m.motorcycle_id ORDER BY m.date DESC,m.id DESC')));
app.post('/api/motorcycles/:id/status',auth,(req,res)=>{if(!deptOnly(req,['D4']))return res.status(403).json({error:'D4 Operations or D1 only'});const m=one('SELECT * FROM motorcycles WHERE id=?',req.params.id);if(!m)return res.status(404).json({error:'Motorcycle not found'});const allowed=['Active','Inactive','Under Maintenance','Sold / Retired'];if(!allowed.includes(req.body.status))return res.status(400).json({error:'Invalid status'});db.prepare('UPDATE motorcycles SET status=? WHERE id=?').run(req.body.status,m.id);log(req,'CHANGE','Motorcycle',m.id,m,{...m,status:req.body.status},req.body.reason||'');res.json({ok:true})});
app.post('/api/daily-closing',auth,(req,res)=>{if(!deptOnly(req,['D4']))return res.status(403).json({error:'D4 Operations or D1 only'});const date=req.body.date||new Date().toISOString().slice(0,10);const income=one('SELECT COALESCE(SUM(amount),0) total FROM income WHERE date=?',date).total;const expenses=one('SELECT COALESCE(SUM(amount),0) total FROM expenses WHERE date=?',date).total;const net=income-expenses;try{const r=db.prepare('INSERT INTO daily_closings(date,income,expenses,net,closed_by,notes) VALUES(?,?,?,?,?,?)').run(date,income,expenses,net,req.user.id,req.body.notes||'');log(req,'CREATE','Daily Closing',r.lastInsertRowid,null,{date,income,expenses,net,notes:req.body.notes||''});res.json({ok:true,id:r.lastInsertRowid,income,expenses,net})}catch(e){res.status(400).json({error:'Closing already exists for this date'})}});
app.get('/api/daily-closings',auth,(req,res)=>res.json(rows('SELECT c.*,u.name closed_by_name FROM daily_closings c JOIN users u ON u.id=c.closed_by ORDER BY c.date DESC')));
app.get('/api/fleet-detail/:id',auth,(req,res)=>{const id=req.params.id;res.json({motorcycle:one('SELECT * FROM motorcycles WHERE id=?',id),income:rows('SELECT i.*,u.name entered_name FROM income i JOIN users u ON u.id=i.entered_by WHERE i.motorcycle_id=? ORDER BY i.date DESC',id),expenses:rows('SELECT e.*,u.name entered_name FROM expenses e JOIN users u ON u.id=e.entered_by WHERE e.motorcycle_id=? ORDER BY e.date DESC',id),maintenance:rows('SELECT * FROM maintenance WHERE motorcycle_id=? ORDER BY date DESC',id),odometer:rows('SELECT o.*,u.name entered_name FROM odometer o JOIN users u ON u.id=o.entered_by WHERE o.motorcycle_id=? ORDER BY date DESC',id),assignments:rows('SELECT * FROM assignments WHERE motorcycle_id=? ORDER BY start_date DESC',id)})});
app.get('/api/evidence',auth,(req,res)=>res.json(rows(d1(req)?'SELECT e.*,u.name uploaded_name FROM evidence e JOIN users u ON u.id=e.uploaded_by ORDER BY e.id DESC':'SELECT e.*,u.name uploaded_name FROM evidence e JOIN users u ON u.id=e.uploaded_by WHERE u.department_id=? ORDER BY e.id DESC',...(d1(req)?[]:[req.user.department_id]))));
app.get('/api/alerts',auth,(req,res)=>{const today=new Date().toISOString().slice(0,10);const out=[];rows('SELECT * FROM motorcycles WHERE status="Under Maintenance"').forEach(m=>out.push({level:'danger',text:`${m.code} is under maintenance`}));rows('SELECT * FROM maintenance WHERE next_service IS NOT NULL').forEach(m=>{if(m.next_service<=today)out.push({level:'warning',text:`${m.motorcycle_id} maintenance service due/overdue`})});rows('SELECT * FROM tasks WHERE status<>"Completed" AND deadline IS NOT NULL AND deadline<?',today).forEach(t=>out.push({level:'danger',text:`Task overdue: ${t.name}`}));res.json(out)});


// Account management: D1 only. Department reassignment preserves the same user account.
app.get('/api/users',auth,(req,res)=>{if(!d1(req))return res.status(403).json({error:'Only D1 can manage accounts'});res.json(rows('SELECT id,name,username,department_id,active,created_at FROM users ORDER BY id'))});
app.post('/api/users',auth,(req,res)=>{if(!d1(req))return res.status(403).json({error:'Only D1 can create accounts'});const {name,username,password,department_id}=req.body;if(!name||!username||!password||!department_id||password.length<10)return res.status(400).json({error:'Name, username, department and password (10+ chars) are required'});try{const r=db.prepare('INSERT INTO users(name,username,password_hash,department_id) VALUES(?,?,?,?)').run(name.trim(),username.trim().toLowerCase(),bcrypt.hashSync(password,12),department_id);log(req,'CREATE','User',r.lastInsertRowid,null,{name,username,department_id});res.json({ok:true,id:r.lastInsertRowid})}catch(e){res.status(400).json({error:'Username already exists or invalid department'})}});
app.patch('/api/users/:id',auth,(req,res)=>{if(!d1(req))return res.status(403).json({error:'Only D1 can update accounts'});const u=one('SELECT * FROM users WHERE id=?',req.params.id);if(!u)return res.status(404).json({error:'User not found'});const department_id=req.body.department_id||u.department_id,active=req.body.active===undefined?u.active:(req.body.active?1:0),name=req.body.name||u.name;if(department_id===u.department_id&&active===u.active&&name===u.name&&!req.body.password)return res.json({ok:true});const changed={name,department_id,active};db.prepare('UPDATE users SET name=?,department_id=?,active=?,password_hash=CASE WHEN ? IS NULL THEN password_hash ELSE ? END WHERE id=?').run(name,department_id,active,req.body.password||null,req.body.password?bcrypt.hashSync(req.body.password,12):null,u.id);log(req,'CHANGE','User',u.id,u,changed,req.body.reason||'');res.json({ok:true})});
// D3 verification of daily fleet income. Verification is an accounting control, not an edit.
app.post('/api/income/:id/verify',auth,(req,res)=>{if(!['D1','D3'].includes(req.user.department_id))return res.status(403).json({error:'Only D3 Finance or D1 can verify income'});const i=one('SELECT * FROM income WHERE id=?',req.params.id);if(!i)return res.status(404).json({error:'Income record not found'});db.prepare('UPDATE income SET verified=1 WHERE id=?').run(i.id);log(req,'VERIFY','Fleet Income',i.id,i,{...i,verified:1},req.body.reason||'');res.json({ok:true})});
app.use('/uploads',auth,express.static(path.join(ROOT,'public/uploads')));app.use(express.static(path.join(ROOT,'public'),{index:'index.html'}));app.use((req,res)=>res.sendFile(path.join(ROOT,'public/index.html')));
app.listen(PORT,()=>console.log(`THE BG WEB running on http://localhost:${PORT}`));
