const app=document.getElementById('app');
const state={me:null,data:null,page:'dashboard',activities:[],goals:[],assignments:[],odometer:[],maintenance:[],closings:[],alerts:[],evidence:[]};
const $=id=>document.getElementById(id); const today=()=>new Date().toISOString().slice(0,10);
const money=n=>Number(n||0).toLocaleString()+' RWF';
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function api(url,opt={}){const r=await fetch(url,{credentials:'include',...opt,headers: opt.body instanceof FormData ? {...(opt.headers||{})} : {'Content-Type':'application/json',...(opt.headers||{})}});const j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error||'Request failed');return j}
async function load(){state.data=await api('/api/bootstrap');
 for(const [k,u] of Object.entries({activities:'/api/activities',goals:'/api/goals',assignments:'/api/assignments',odometer:'/api/odometer',maintenance:'/api/maintenance',closings:'/api/daily-closings',alerts:'/api/alerts',evidence:'/api/evidence'})){try{state[k]=await api(u)}catch{state[k]=[]}}
}
async function boot(){try{state.me=(await api('/api/me')).user;await load();render()}catch{login()}}
function login(){app.innerHTML=`<div class="login"><div class="loginbox"><div class="brand">THE BG</div><div class="sub">ONE COMPANY MANAGEMENT PLATFORM</div><h2>Sign in</h2><form onsubmit="doLogin(event)" class="form"><select id="username"><option value="d1">D1 — MANISHIMWE FARADJI</option><option value="d2">D2 — AHMED FAZZIR</option><option value="d3">D3 — NIYITANGA OSAMA</option><option value="d4">D4 — KIREZI NASSIB</option><option value="d5">D5 — IMANANIYOGISUBIZO YUSSUF</option></select><input id="password" type="password" placeholder="Password" required><button class="btn">Sign in</button></form><p class="muted">Production deployment uses the password configured by the administrator.</p></div></div>`}
async function doLogin(e){e.preventDefault();try{await api('/api/login',{method:'POST',body:JSON.stringify({username:$('username').value,password:$('password').value})});await boot()}catch(x){alert(x.message)}}
async function logout(){await api('/api/logout',{method:'POST'});state.me=null;login()}
const nav=[['dashboard','Dashboard'],['departments','Departments'],['tasks','Tasks'],['activities','Daily Work'],['reports','Reports'],['goals','Goals'],['performance','Performance'],['finance','Finance'],['fleet','Motorcycle Fleet'],['evidence','Evidence'],['audit','Audit Trail']];
function shell(title,body){app.innerHTML=`<div class="layout"><aside class="sidebar"><div class="brand">THE BG</div><div class="sub">MANAGEMENT WEB</div><div class="user"><b>${esc(state.me.name)}</b><span>${state.me.department_id}</span></div><div class="nav">${nav.map(([id,t])=>`<button class="${state.page===id?'active':''}" onclick="go('${id}')">${t}</button>`).join('')}<button onclick="logout()">Logout</button></div></aside><main class="main"><header class="top"><div><div class="eyebrow">THE BG WEB</div><h1>${title}</h1></div><span class="badge">${state.me.department_id} · ${esc(state.me.name)}</span></header>${body}</main></div><nav class="mobile">${[['dashboard','Home'],['tasks','Tasks'],['finance','Finance'],['fleet','Fleet'],['audit','Audit']].map(([id,t])=>`<button onclick="go('${id}')">${t}</button>`).join('')}</nav>`}
function go(p){state.page=p;render()}; const card=(a,b)=>`<div class="card stat"><span>${a}</span><strong>${b}</strong></div>`;
function dashboard(){const d=state.data, inc=d.income.reduce((a,x)=>a+Number(x.amount),0),exp=d.expenses.reduce((a,x)=>a+Number(x.amount),0),done=d.tasks.filter(x=>x.status==='Completed').length,over=d.tasks.filter(x=>x.status!=='Completed'&&x.deadline&&x.deadline<today()).length;shell('THE BG TODAY',`<div class="grid">${card('Active Users',d.users.filter(x=>x.active).length)}${card('Tasks',d.tasks.length)}${card('Completed',done)}${card('Overdue',over)}${card('Income',money(inc))}${card('Expenses',money(exp))}${card('Net Result',money(inc-exp))}${card('Motorcycles',d.motorcycles.length)}</div><div class="section grid2"><div class="card"><h2>Department Performance</h2>${d.departments.map(x=>{const ts=d.tasks.filter(t=>t.responsible_name===x.person), sc=ts.length?Math.round(ts.filter(t=>t.status==='Completed').length/ts.length*100):0;return `<div class="row"><b>${x.id}</b><span>${esc(x.person)}</span><b>${sc}%</b></div>`}).join('')}</div><div class="card"><h2>Alerts</h2>${state.alerts.length?state.alerts.slice(0,8).map(a=>`<div class="alert ${a.level}">${esc(a.text)}</div>`).join(''):'<p class="muted">No active alerts.</p>'}</div></div>`)}
function departments(){shell('Departments',`<div class="grid">${state.data.departments.map(d=>{const mine=d.id===state.me.department_id;return `<div class="card"><span class="eyebrow">${d.id}${mine?' · YOUR WORKSPACE':''}</span><h2>${esc(d.position)}</h2><h3>${esc(d.person)}</h3><p>${esc(d.responsibility)}</p><div class="tag">One responsible person</div><div class="row"><span>Tasks</span><b>${state.data.tasks.filter(t=>t.responsible_name===d.person).length}</b></div><div class="row"><span>Reports</span><b>${state.data.reports.filter(r=>r.user_name===d.person).length}</b></div></div>`}).join('')}</div>${state.me.department_id==='D1'?`<div class="section card"><h2>Accounts</h2><p class="muted">D1 account controls are available through the secure API for production administration.</p></div>`:''}`)}
function tasks(){const users=state.data.users.filter(u=>u.active&& (state.me.department_id==='D1'||u.department_id===state.me.department_id));shell('Tasks',`<div class="card"><h2>Create Task</h2><form class="form" onsubmit="createTask(event)"><div class="two"><input id="tn" placeholder="Task name" required><select id="tu">${users.map(u=>`<option value="${u.id}">${u.department_id} — ${esc(u.name)}</option>`).join('')}</select></div><div class="two"><input id="ts" type="date" value="${today()}"><input id="td" type="date"></div><div class="two"><select id="tp"><option>Normal</option><option>High</option><option>Low</option></select><textarea id="tx" placeholder="Description"></textarea></div><button class="btn">Create Task</button></form></div><div class="card section tablewrap"><table class="table"><tr><th>Task</th><th>Responsible</th><th>Deadline</th><th>Priority</th><th>Status</th></tr>${state.data.tasks.map(t=>`<tr><td><b>${esc(t.name)}</b><small>${esc(t.description||'')}</small></td><td>${esc(t.responsible_name)}</td><td>${t.deadline||'-'}</td><td>${t.priority}</td><td><select onchange="changeTask(${t.id},this.value)">${['Not Started','In Progress','Completed','Delayed'].map(s=>`<option ${s===t.status?'selected':''}>${s}</option>`).join('')}</select></td></tr>`).join('')}</table></div>`)}
async function createTask(e){e.preventDefault();try{await api('/api/tasks',{method:'POST',body:JSON.stringify({name:$('tn').value,responsible_user:Number($('tu').value),start_date:$('ts').value,deadline:$('td').value,priority:$('tp').value,description:$('tx').value})});await refresh()}catch(x){alert(x.message)}}
async function changeTask(id,status){try{await api('/api/tasks/'+id,{method:'PATCH',body:JSON.stringify({status})});await refresh()}catch(x){alert(x.message)}}
function activities(){shell('Daily Work',`<div class="card"><h2>Daily Activity</h2><form class="form" onsubmit="saveActivity(event)"><input id="adate" type="date" value="${today()}"><textarea id="adone" placeholder="What did you complete today?" required></textarea><textarea id="aunfinished" placeholder="What remains unfinished?"></textarea><textarea id="areason" placeholder="Reason if unfinished"></textarea><input id="atime" type="number" min="0" step="0.5" placeholder="Time spent (hours)"><button class="btn">Save Daily Activity</button></form></div><div class="section grid">${state.activities.slice(0,30).map(a=>`<div class="card"><b>${esc(a.user_name)}</b> · ${a.date}<p>${esc(a.done)}</p><small>${esc(a.unfinished||'')}</small></div>`).join('')}</div>`)}
async function saveActivity(e){e.preventDefault();try{await api('/api/activities',{method:'POST',body:JSON.stringify({date:$('adate').value,done:$('adone').value,unfinished:$('aunfinished').value,reason:$('areason').value,time_spent:Number($('atime').value||0)})});await refresh()}catch(x){alert(x.message)}}
function reports(){shell('Reports',`<div class="card"><form class="form" onsubmit="createReport(event)"><div class="two"><select id="rt"><option>Daily Report</option><option>Weekly Report</option><option>Monthly Report</option><option>Project Report</option><option>Financial Report</option></select><input id="rdate" type="date" value="${today()}"></div><textarea id="rb" rows="7" placeholder="Write report..." required></textarea><button class="btn">Submit Report</button></form></div><div class="section grid">${state.data.reports.map(r=>`<div class="card"><span class="tag">${esc(r.type)}</span><p>${esc(r.body)}</p><small>${esc(r.user_name)} · ${r.date} · ${r.status}</small></div>`).join('')}</div>`)}
async function createReport(e){e.preventDefault();try{await api('/api/reports',{method:'POST',body:JSON.stringify({type:$('rt').value,body:$('rb').value,date:$('rdate').value})});await refresh()}catch(x){alert(x.message)}}
function goals(){shell('Goals & Objectives',`<div class="card"><form class="form" onsubmit="createGoal(event)"><div class="two"><input id="gt" placeholder="Goal title" required><select id="gs"><option>Department</option><option>Individual</option><option>Monthly</option><option>Yearly</option></select></div><div class="two"><input id="gperiod" placeholder="Period e.g. Sep 2026"><input id="gtarget" type="number" min="1" value="100" placeholder="Target"></div><button class="btn">Create Goal</button></form></div><div class="section grid">${state.goals.map(g=>{const pct=Math.min(100,Math.round(Number(g.achieved)/Math.max(1,Number(g.target))*100));return `<div class="card"><b>${esc(g.title)}</b><p>${g.scope} · ${g.period||'No period'}</p><div class="progress"><i style="width:${pct}%"></i></div><div class="row"><span>${g.achieved} / ${g.target}</span><b>${pct}%</b></div><button class="btn light" onclick="updateGoal(${g.id},${g.achieved})">Update achievement</button></div>`}).join('')}</div>`)}
async function createGoal(e){e.preventDefault();try{await api('/api/goals',{method:'POST',body:JSON.stringify({title:$('gt').value,scope:$('gs').value,period:$('gperiod').value,target:Number($('gtarget').value),achieved:0,department_id:state.me.department_id})});await refresh()}catch(x){alert(x.message)}}
async function updateGoal(id,current){const v=prompt('New achieved value:',current);if(v===null)return;try{await api('/api/goals/'+id,{method:'PATCH',body:JSON.stringify({achieved:Number(v)})});await refresh()}catch(x){alert(x.message)}}
function performance(){const d=state.data,s=d.users.map(u=>{const t=d.tasks.filter(x=>x.responsible_user===u.id),done=t.filter(x=>x.status==='Completed'),del=t.filter(x=>x.status==='Delayed');const a=state.activities.filter(x=>x.user_id===u.id);const r=d.reports.filter(x=>x.user_id===u.id);const taskScore=t.length?(done.length/t.length)*60:0,ontime=t.length?(done.filter(x=>!x.deadline||x.deadline>=x.start_date).length/t.length)*20:0,act=a.length?10:0,rep=r.length?10:0;return {u,score:Math.round(Math.min(100,taskScore+ontime+act+rep)),del:del.length}}).sort((a,b)=>b.score-a.score);shell('Performance',`<div class="card"><h2>System Data Ranking</h2><p class="muted">Score uses task completion, completion timing, activity and reports. Weighting can be refined without changing stored history.</p><div class="tablewrap"><table class="table"><tr><th>#</th><th>Person</th><th>Department</th><th>Score</th><th>Delayed</th></tr>${s.map((x,i)=>`<tr><td>${i+1}</td><td>${esc(x.u.name)}</td><td>${x.u.department_id}</td><td><b>${x.score}%</b></td><td>${x.del}</td></tr>`).join('')}</table></div></div>`)}
function finance(){const d=state.data,inc=d.income.reduce((a,x)=>a+Number(x.amount),0),exp=d.expenses.reduce((a,x)=>a+Number(x.amount),0);shell('Finance',`<div class="grid">${card('Total Income',money(inc))}${card('Total Expenses',money(exp))}${card('Net Result',money(inc-exp))}${card('Pending Changes',d.changes.length)}</div><div class="section card"><h2>Add Expense</h2><form class="form" onsubmit="addExpense(event)"><div class="two"><input id="edate" type="date" value="${today()}" required><input id="eamount" type="number" min="0.01" step="0.01" placeholder="Amount (RWF)" required></div><div class="two"><select id="etype"><option>Fuel</option><option>Maintenance</option><option>Repairs</option><option>Salary / Commission</option><option>Other</option></select><select id="emoto"><option value="">Company expense / no motorcycle</option>${d.motorcycles.map(m=>`<option value="${m.id}">${esc(m.code)} — ${esc(m.plate||'')}</option>`).join('')}</select></div><input id="edesc" placeholder="Description"><button class="btn">Save Expense</button></form></div>${state.me.department_id==='D1'?`<div class="section card"><h2>Pending Finance Changes</h2>${d.changes.length?d.changes.map(c=>`<div class="change"><b>${esc(c.record_type)} #${c.record_id}</b><p>${esc(c.reason)}</p><button class="btn" onclick="decision(${c.id},'Approved')">Approve</button> <button class="btn danger" onclick="decision(${c.id},'Rejected')">Reject</button></div>`).join(''):'<p class="muted">No pending changes.</p>'}</div>`:''}<div class="section card tablewrap"><table class="table"><tr><th>Date</th><th>Type</th><th>Moto</th><th>Amount</th><th>By</th></tr>${d.expenses.map(x=>`<tr><td>${x.date}</td><td>${esc(x.expense_type)}</td><td>${esc(x.motorcycle_code||'-')}</td><td>${money(x.amount)}</td><td>${esc(x.entered_name)}</td></tr>`).join('')}</table></div>`)}
async function addExpense(e){e.preventDefault();try{await api('/api/expenses',{method:'POST',body:JSON.stringify({date:$('edate').value,motorcycle_id:$('emoto').value?Number($('emoto').value):null,expense_type:$('etype').value,amount:Number($('eamount').value),description:$('edesc').value})});await refresh()}catch(x){alert(x.message)}}
async function decision(id,decision){const note=prompt('Decision note:')||'';try{await api('/api/finance-changes/'+id+'/decision',{method:'POST',body:JSON.stringify({decision,note})});await refresh()}catch(x){alert(x.message)}}
function fleet(){const d=state.data,fs=state.fleet||{};shell('Motorcycle Fleet',`<div class="grid">${card('Total',fs.totalMotorcycles??d.motorcycles.length)}${card('Active',fs.active??0)}${card('Maintenance',fs.maintenance??0)}${card("Today's Income",money(fs.todayIncome))}${card("Today's Expenses",money(fs.todayExpenses))}${card("Today's Net",money(fs.todayNet))}${card('Fleet Net',money(fs.net))}</div><div class="section grid2"><div class="card"><h2>Register Motorcycle</h2><form class="form" onsubmit="addMoto(event)"><input id="mcode" placeholder="Motorcycle ID / Number" required><input id="mplate" placeholder="Plate Number"><input id="mmodel" placeholder="Model"><div class="two"><input id="mpdate" type="date"><input id="mprice" type="number" min="0" placeholder="Purchase Price"></div><button class="btn">Register</button></form></div><div class="card"><h2>Daily Closing</h2><form class="form" onsubmit="closeDay(event)"><input id="cdate" type="date" value="${today()}"><textarea id="cnotes" placeholder="Closing notes"></textarea><button class="btn">Close Day</button></form></div></div><div class="section card tablewrap"><h2>Motorcycles</h2><table class="table"><tr><th>Moto</th><th>Plate</th><th>Model</th><th>Status</th><th>Actions</th></tr>${d.motorcycles.map(m=>`<tr><td><b>${esc(m.code)}</b></td><td>${esc(m.plate||'-')}</td><td>${esc(m.model||'-')}</td><td>${m.status}</td><td><button class="btn light" onclick="viewMoto(${m.id})">History</button> ${state.me.department_id==='D4'||state.me.department_id==='D1'?`<button class="btn light" onclick="addIncome(${m.id})">Income</button>`:''}</td></tr>`).join('')}</table></div><div class="section card"><h2>Odometer / Mileage</h2><form class="form" onsubmit="addOdometer(event)"><div class="two"><select id="om">${d.motorcycles.map(m=>`<option value="${m.id}">${esc(m.code)}</option>`).join('')}</select><input id="odate" type="date" value="${today()}" required></div><input id="omile" type="number" min="0" step="0.1" placeholder="Current mileage" required><button class="btn">Save Mileage</button></form></div><div class="section grid2"><div class="card"><h2>Rider Assignment</h2><form class="form" onsubmit="assignRider(event)"><select id="am">${d.motorcycles.map(m=>`<option value="${m.id}">${esc(m.code)}</option>`).join('')}</select><input id="ar" placeholder="Rider / worker name" required><input id="as" type="date" value="${today()}"><input id="an" placeholder="Notes"><button class="btn">Assign</button></form></div><div class="card"><h2>Maintenance</h2><form class="form" onsubmit="addMaintenance(event)"><select id="mm">${d.motorcycles.map(m=>`<option value="${m.id}">${esc(m.code)}</option>`).join('')}</select><input id="mi" placeholder="Problem / issue" required><div class="two"><input id="md" type="date" value="${today()}"><input id="mileage" type="number" min="0" placeholder="Mileage"></div><input id="mparts" placeholder="Parts used"><div class="two"><input id="mcost" type="number" min="0" placeholder="Cost"><input id="mgarage" placeholder="Garage / mechanic"></div><div class="two"><input id="mnext" type="date"><input id="mdown" type="number" min="0" placeholder="Downtime (hours)"></div><select id="mstatus"><option>Completed</option><option>In Progress</option></select><button class="btn">Save Maintenance</button></form></div></div><div class="section card tablewrap"><h2>Recent Income</h2><table class="table"><tr><th>Date</th><th>Moto</th><th>Amount</th><th>Entered by</th><th>Verified</th></tr>${d.income.slice(0,50).map(x=>`<tr><td>${x.date}</td><td>${esc(x.motorcycle_code)}</td><td>${money(x.amount)}</td><td>${esc(x.entered_name)}</td><td>${x.verified?'Verified':(state.me.department_id==='D3'||state.me.department_id==='D1'?`<button class="btn light" onclick="verifyIncome(${x.id})">Verify</button>`:'Pending')}</td></tr>`).join('')}</table></div>`)}
async function addMoto(e){e.preventDefault();try{await api('/api/motorcycles',{method:'POST',body:JSON.stringify({code:$('mcode').value,plate:$('mplate').value,model:$('mmodel').value,purchase_price:Number($('mprice').value||0),purchase_date:$('mpdate').value,status:'Active'})});await refresh()}catch(x){alert(x.message)}}
async function verifyIncome(id){try{await api('/api/income/'+id+'/verify',{method:'POST',body:JSON.stringify({reason:'Finance verification'})});await refresh()}catch(x){alert(x.message)}}
async function addIncome(id){const amount=prompt('Daily income (RWF):');if(amount===null)return;const note=prompt('Collection note (optional):')||'';try{await api('/api/income',{method:'POST',body:JSON.stringify({date:today(),motorcycle_id:id,amount:Number(amount),collection_note:note})});await refresh()}catch(x){alert(x.message)}}
async function assignRider(e){e.preventDefault();try{await api('/api/assignments',{method:'POST',body:JSON.stringify({motorcycle_id:Number($('am').value),rider_name:$('ar').value,start_date:$('as').value,notes:$('an').value})});await refresh()}catch(x){alert(x.message)}}
async function addOdometer(e){e.preventDefault();try{await api('/api/odometer',{method:'POST',body:JSON.stringify({motorcycle_id:Number($('om').value),date:$('odate').value,mileage:Number($('omile').value)})});await refresh()}catch(x){alert(x.message)}}
async function addMaintenance(e){e.preventDefault();try{await api('/api/maintenance',{method:'POST',body:JSON.stringify({motorcycle_id:Number($('mm').value),issue:$('mi').value,date:$('md').value,mileage:$('mileage').value,parts:$('mparts').value,cost:Number($('mcost').value||0),garage:$('mgarage').value,next_service:$('mnext').value,downtime:Number($('mdown').value||0),status:$('mstatus').value})});await refresh()}catch(x){alert(x.message)}}
async function closeDay(e){e.preventDefault();try{const r=await api('/api/daily-closing',{method:'POST',body:JSON.stringify({date:$('cdate').value,notes:$('cnotes').value})});alert(`Closed ${r.date||$('cdate').value}: ${money(r.net)}`);await refresh()}catch(x){alert(x.message)}}
async function viewMoto(id){try{const r=await api('/api/fleet-detail/'+id);const text=[`MOTORCYCLE ${r.motorcycle.code}`,`Status: ${r.motorcycle.status}`,`Income records: ${r.income.length}`,`Expense records: ${r.expenses.length}`,`Maintenance records: ${r.maintenance.length}`,`Odometer records: ${r.odometer.length}`,`Assignments: ${r.assignments.length}`].join('\n');alert(text)}catch(x){alert(x.message)}}
function evidence(){shell('Evidence',`<div class="card"><h2>Upload Evidence</h2><form class="form" onsubmit="uploadEvidence(event)"><input id="efile" type="file" required><select id="etask"><option value="">No task link</option>${state.data.tasks.map(t=>`<option value="${t.id}">${esc(t.name)}</option>`).join('')}</select><button class="btn">Upload</button></form></div><div class="section grid">${state.evidence.map(e=>`<div class="card"><b>${esc(e.original_name)}</b><p>${esc(e.uploaded_name)} · ${e.uploaded_at}</p><a class="btn light" href="/uploads/${encodeURIComponent(e.filename)}" target="_blank">Open</a></div>`).join('')}</div>`)}
async function uploadEvidence(e){e.preventDefault();const fd=new FormData();fd.append('file',$('efile').files[0]);if($('etask').value)fd.append('task_id',$('etask').value);try{const r=await fetch('/api/evidence',{method:'POST',body:fd,credentials:'include'});const j=await r.json();if(!r.ok)throw Error(j.error);await refresh()}catch(x){alert(x.message)}}
function audit(){shell('Audit Trail',`<div class="card"><input id="aq" placeholder="Search all audit information..." oninput="filterAudit()"><div id="auditbox" class="tablewrap" style="margin-top:14px"></div></div>`);filterAudit()}
function filterAudit(){const q=($('aq')?.value||'').toLowerCase(),a=state.data.audit.filter(x=>JSON.stringify(x).toLowerCase().includes(q));$('auditbox').innerHTML=`<table class="table"><tr><th>Date/Time</th><th>Action</th><th>Record</th><th>ID</th><th>Who</th><th>Reason</th></tr>${a.map(x=>`<tr><td>${x.when_at}</td><td>${x.action}</td><td>${x.record_type}</td><td>${x.record_id||'-'}</td><td>${esc(x.user_name||'-')}</td><td>${esc(x.reason||'')}</td></tr>`).join('')}</table>`}
async function refresh(){await load();try{state.fleet=await api('/api/fleet-summary')}catch{}render()}
function render(){if(!state.me)return login();({dashboard,departments,tasks,activities,reports,goals,performance,finance,fleet,evidence,audit}[state.page]||dashboard)()}
(async()=>{try{state.me=(await api('/api/me')).user;await load();state.fleet=await api('/api/fleet-summary');render()}catch{login()}})();
